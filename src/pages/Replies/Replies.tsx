import { CommentList } from '../../components/Replies';
import { RepliesContainer } from './styled';

export const Replies = () => {
  return (
    <RepliesContainer>
      <CommentList />
    </RepliesContainer>
  );
};
