import type { QuestionDataType } from "stores/page-store";

export const SearchArticles:React.FC<QuestionDataType> = (data) :JSX.Element => {
  console.log(data.answers);
  return (
    <>
    </>
  );
};